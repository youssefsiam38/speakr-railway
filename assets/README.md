# Assets

- `icon.png` — the template's own generic icon (a simple soundwave). It does **not** reproduce the Speakr logo or
  any Speakr brand identifier. Referenced by the published template via a raw GitHub URL to a tagged commit (see
  `RAILWAY_TEMPLATE.md`).
